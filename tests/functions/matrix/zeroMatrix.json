{
  "name": "Zero matrix function test",
  "version": 2,
  "process": [
    {
      "desc": "argument of integer",
      "config": {},
      "tests": [
        {
          "input": ["zeromat(0)"],
          "type": "equals",
          "output": "[]"
        },
        {
          "input": ["zeromat(1)"],
          "type": "equals",
          "output": "[0]"
        },
        {
          "input": ["zeromat(2)"],
          "type": "equals",
          "output": "[0,0;0,0]"
        }
      ]
    },
    {
      "desc": "argument of integer and integer",
      "config": {},
      "tests": [
        {
          "input": ["zeromat(2,3)"],
          "type": "equals",
          "output": "[0,0,0;0,0,0]"
        }
      ]
    }
  ]
}
